/**
 * Build-time constants injected via Vite's `define` config.
 * Values are resolved from environment variables at build time.
 * See web/vite.config.ts and web/scripts/colony-config.ts.
 */

declare const __COLONY_GITHUB_URL__: string;
declare const __COLONY_FRAMEWORK_URL__: string;
declare const __COLONY_FRAMEWORK_NAME__: string;
